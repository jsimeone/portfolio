// import * as process from ".svelte-kit/ambient.js";

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
    // ...(process.env.NODE_ENV === 'production' ? {cssnano: {} }: {})
  },
}
